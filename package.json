{
  "name": "13-advanced-dom-bankist",
  "version": "1.0.0",
  "description": "banking meets minimalist",
  "default": "index.js",
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "setup": "pm2 start --name mk-bankist-minimalist-banking npm -- start",
    "start": "npm run build && parcel serve ./src/index.html",
    "deploy": "git pull --no-rebase && npm i && npm run build && pm2 restart mk-bankist-minimalist-banking && pm2 save",
    "build": "parcel build ./src/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "babel-loader": "^9.2.1",
    "parcel": "^2.13.3",
    "svgo": "^3.3.2"
  },
  "dependencies": {
    "css-loader": "^7.1.2",
    "style-loader": "^4.0.0"
  }
}
